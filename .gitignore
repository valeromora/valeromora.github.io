# Privacy: cv_english.pdf contains the phone number and email — must never be
# committed to the public valeromora.github.io repository.
cv_english.pdf

# Local agent workspace (research notes, drafts) — never deployed.
.atl/

# Platform / editor / tooling noise
.DS_Store
node_modules/
*.swp